import type { LucideIcon } from "lucide-react";
export function EmptyModule({eyebrow,title,description,icon:Icon,items}:{eyebrow:string;title:string;description:string;icon:LucideIcon;items:string[]}){return <div className="page-stack"><section className="page-heading"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{description}</p></div></section><section className="empty-module panel"><span><Icon size={30}/></span><h2>Estructura preparada</h2><p>Este módulo se conectará después de validar su funcionamiento y sus permisos.</p><div>{items.map(item=><span key={item}>{item}</span>)}</div></section></div>}
